services:
  - type: web
    name: acostaspool-web
    runtime: node
    plan: starter
    healthCheckPath: /api/health
    buildCommand: |
      set -e
      npm install --include=dev
      npx prisma generate
      npx playwright install chromium
      npm run build
    preDeployCommand: |
      for i in 1 2 3; do
        npx prisma migrate deploy && break
        echo "Prisma migrate deploy failed (attempt $i). Retrying in 5s..."
        sleep 5
      done
    startCommand: npm run start -- -H 0.0.0.0 -p $PORT
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: NODE_ENV
        value: production
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: "0"
      - key: DATABASE_URL
        fromDatabase:
          name: acostaspool-db
          property: connectionString
      - key: AUTH_SECRET
        sync: false
      - key: APP_URL
        value: https://acostaspool.com
      - key: STORAGE_DRIVER
        value: s3
      - key: NEXT_PUBLIC_CDN_URL
        sync: false
      - key: AWS_REGION
        sync: false
      - key: AWS_S3_BUCKET
        sync: false
      - key: AWS_ACCESS_KEY_ID
        sync: false
      - key: AWS_SECRET_ACCESS_KEY
        sync: false
      - key: NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
        sync: false
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        sync: false
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
      - key: SMTP_FROM
        sync: false
      - key: CRON_SECRET
        sync: false
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false

  - type: worker
    name: acostaspool-cron-worker
    runtime: node
    plan: starter
    buildCommand: |
      set -e
      npm install --include=dev
      npx prisma generate
    startCommand: npm run worker:cron
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: NODE_ENV
        value: production
      - key: PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD
        value: "1"
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: "0"
      - key: DATABASE_URL
        fromDatabase:
          name: acostaspool-db
          property: connectionString
      - key: APP_URL
        value: https://acostaspool.com
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        sync: false
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
      - key: SMTP_FROM
        sync: false
      - key: CRON_SECRET
        sync: false
      - key: STRIPE_SECRET_KEY
        sync: false

databases:
  - name: acostaspool-db
    plan: free
    databaseName: acostaspool
